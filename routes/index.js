/* 

import express from 'express';
import routerPost from './post.router.js'
import routerComment from './comments.router.js'

//express.js 라우터 생성

const router = express.Router();

// JSON 파싱 미들웨어 예시
app.use(express.json());

// 다른 라우터들을 사용합니다.
app.use('/', routerPost);
app.use('/comments', routerComment);



export default router; */